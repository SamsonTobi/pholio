{
  "name": "@pholio/mcp-server",
  "version": "0.1.0",
  "description": "Thin MCP client entry pointing at PHOLIO_BASE_URL (/api/mcp). Transport is plain JSON-RPC over HTTPS (MCP Streamable HTTP compatible); @modelcontextprotocol/sdk adoption is a tracked follow-up, not required.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "pholio-mcp-server": "./bin/cli.js"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "start": "bun src/index.ts",
    "test": "bunx vitest run"
  },
  "dependencies": {
    "@pholio/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
